#ifndef _CAMERA_H
#define _CAMERA_H


class Camera
{
    public:
        Camera();
        virtual ~Camera();
        Camera(const Camera& other);

    protected:

    private:
};

#endif // _CAMERA_H
